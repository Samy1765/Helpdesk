# Web
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Database
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.30
aiosqlite>=0.20
alembic>=1.13

# Auth
python-jose[cryptography]>=3.3
bcrypt>=4.1
email-validator>=2.1

# Validation / config
pydantic>=2.7
pydantic-settings>=2.3

# LLM HTTP clients (providers are called over plain HTTP; no vendor SDK lock-in)
httpx>=0.27

# Embeddings & vector search
numpy>=1.26
faiss-cpu>=1.8
sentence-transformers>=3.0

# Logging
structlog>=24.1
greenlet>=3.0          # required by SQLAlchemy's async engine
